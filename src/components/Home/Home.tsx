import React, { ReactNode } from 'react';
import DashboardLayout from '../Dashboard';

const Home = (): ReactNode => (
  <DashboardLayout>
    <div>Home Page</div>
  </DashboardLayout>
);

export default Home;
