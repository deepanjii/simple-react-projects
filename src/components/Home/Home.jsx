/* @flow */
import React from 'react';
import type { Node } from 'react';
import DashboardLayout from '../Dashboard';

const Home = (): Node => (
  <DashboardLayout>
    <div>Home Page</div>
  </DashboardLayout>
);

export default Home;
